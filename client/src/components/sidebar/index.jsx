import './styles.css';

import Identity from '../../newComponents/identity';

const SideBar = () => (
  
  <section className='sidebar'>
    <Identity />
  </section>
);

export default SideBar;
